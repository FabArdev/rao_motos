/.phpunit.cache
/node_modules
# /public/build SÍ se versiona a propósito: así el servidor recibe el frontend
# ya compilado con el `git pull` y no hace falta correr npm allá.
# Antes de hacer push, si tocaste algo de resources/js: npm run build
/public/hot
/public/storage
/storage/*.key
/vendor

.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

# OJO: vite.config.js NO va aquí. Sin él el servidor no puede compilar el
# frontend con `npm run build` y el despliegue automático (deploy.sh) falla.
